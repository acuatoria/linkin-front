[build.environment]
  NPM_FLAGS = "--version"
  NODE_VERSION = "16"

[build]
  publish = "dist"
  command = "npx nuxi build --modern=server --target=es2015"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
